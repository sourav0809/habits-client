/** Default display name when user is not loaded (e.g. in Header before auth) */
export const DEFAULT_USER_NAME = "User";

/** Breakpoint (px) below which layout is treated as mobile (sidebar becomes overlay). */
export const MOBILE_BREAKPOINT = 768;
